import React from 'react';

const ErrorIndicator = () => {
    return (
        <div className='error-indicator' >
            <span>something has gone terribly wrong</span>
            <span>( but we already sent support to fix it )</span>
        </div>
    );
};

export default ErrorIndicator;
